# app-service.yaml
apiVersion: v1
kind: Service
metadata:
    name: myhomelab
spec:
    selector:
        app: myhomelab
    ports:
        - protocol: TCP
          port: 80
          targetPort: 8081
    type: LoadBalancer
